// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

// structs1.sol
// Make me compile!

contract Structs1 {
    // TODO: Define a struct named `User` with two properties:
    // 1. A `string` named `username`
    // 2. A `uint256` named `score`
    

    User public currentUser;

    function setUser(string memory _username, uint256 _score) public {
        // TODO: Initialize `currentUser` with the provided arguments.
        
    }
}
