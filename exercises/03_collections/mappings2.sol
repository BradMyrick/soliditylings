// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

// mappings2.sol
// Make me compile!

contract NestedMappings {
    // TODO: Declare a public nested mapping `allowance` to track token approvals.
    // It should map: owner `address` -> spender `address` -> amount `uint256`

    function getAllowance(address owner, address spender) public view returns (uint256) {
        // TODO: Return the allowance that `owner` gave to `spender`.
        return 0; // REPLACE ME
    }
}
