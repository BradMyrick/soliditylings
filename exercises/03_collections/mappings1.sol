// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

// mappings1.sol
// Make me compile!

contract Mappings1 {
    // TODO: Declare a public mapping that maps `address` to `uint256` named `balances`.

    function setBalance(address user, uint256 amount) public {
        // TODO: Give the `user` a balance of `amount`.
        
    }
}
